% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volumes.R
\name{resolve_volume}
\alias{resolve_volume}
\title{Mounted volumes matching a pattern}
\usage{
resolve_volume(
  pattern,
  mount_root = getOption("researchpaths.mount_root", "/Volumes"),
  require_unique = TRUE
)
}
\arguments{
\item{pattern}{[character] volume-name glob, e.g. "ExternalDrive*".}

\item{mount_root}{[character] where volumes appear. "/Volumes" on macOS,
"/media"/"/mnt" elsewhere; parameterised so this is testable without one.}

\item{require_unique}{[logical] stop when more than one matches. FALSE when a
caller will disambiguate by validating what is ON each volume -- which is
the decoy case: two volumes match, only one holds a real database.}
}
\value{
[character] absolute paths to matching volume roots.
}
\description{
Mounted volumes matching a pattern
}
